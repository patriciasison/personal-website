import Typography from "typography"

const typography = new Typography({
  baseFontSize: "10px",
  googleFonts: [{ name: "Roboto", styles: [400, 500, 700, 900] }],
  headerFontFamily: ["Roboto"],
  bodyFontFamily: ["Roboto"],
})

export default typography
